name: chem-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - rdkit=2023.09
  - numpy<<2          # conda 侧锁定
  - pip
  - pip:
    - numpy<<2        # ← 新增：强制 pip 侧也锁定 1.x
    - streamlit>=1.40.0
    - scikit-learn>=1.3.0
    - joblib>=1.3.0
    - openai>=1.0.0
    - requests>=2.31.0
    - python-dotenv>=1.0.0
    - shap
